name: fitnessapp
description: AI-powered Fitness & Calorie Tracker with Firebase backend.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.4.0
  google_sign_in: ^6.2.1
  flutter_svg: ^2.0.7

  # State Management
  provider: ^6.1.2

  # Networking
  http: ^1.2.2

  # Charts
  fl_chart: ^0.68.0

  # UI & Animations
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.0
  percent_indicator: ^4.2.3
  shimmer: ^3.0.0
  lottie: ^3.1.2
  cached_network_image: ^3.3.1

  # Local Storage
  shared_preferences: ^2.3.2

  # Utilities
  intl: ^0.19.0
  uuid: ^4.4.0
  flutter_local_notifications: ^17.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
